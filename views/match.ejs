<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title><%= title%></title>
      <link rel="shortcut icon" href="/img/favicon.ico"/>
      <link rel="stylesheet" href="/css/style.css">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.css">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
         integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   </head>
   <body>
      <%- include('header'); -%>
      <div class="contain">
         <h1 class="text-white" ><%= text%></h1>
         <div id="owl-carousel" class="owl-carousel owl-theme">
            <% for(let match=0; match< data.data.length; match++) { %>
            <div class="item" >
               <div class="card text-white" onclick="redirectToMatch('<%= data.data[match].match_id %>')" style="background-color: #3B0344; padding: 10px; border-radius: 5px; border:#C500E1 1px solid;">
                  <div>
                     <% if(data.data[match].series) {%>
                     <% if(data.data[match].match_status) {%>
                     <div class="row">
                        <div class="card-title col-9" style="font-size: 13px;">
                           <%= data.data[match].series%>
                        </div>
                        <% if (data.data[match].match_status === 'Live') { %>
                           <div class="card-title text-end col-3 statusLive fw-bold" style="font-size: 13px;">
                               <%= data.data[match].match_status %>
                           </div>
                        <% } else { %>
                           <div class="card-title text-end col-3 statusUpcoming fw-bold" style="font-size: 13px;">
                               <%= data.data[match].match_status %>
                           </div>
                        <% } %>
                     </div>
                     <% } %>
                     <% } %>
                     <div class="row" style="margin-bottom: 5px;">
                        <div class="col-12">
                           <span style="font-size: 10px;"><%= data.data[match].venue %></span>
                        </div>
                     </div>
                     <div class="row">
                        <div class="card-title col-4">
                           <% if(data.data[match].team_a_img) {%>
                           <img src="<%=data.data[match].team_a_img%>" style='height: 40px; width: 75px; object-fit: cover;' alt="No Image">
                           <% } %>
                           <% if(data.data[match].team_a_short) {%>
                           <%= data.data[match].team_a_short %>
                           <% } %>        
                        </div>
                        <div class="card-title col-4">
                           <img src="/img/VS.png" style='height: 45px; width: 45px;' alt="No Image">
                        </div>
                        <div class="card-title col-4">
                           <% if(data.data[match].team_b_img) {%>
                           <img src="<%=data.data[match].team_b_img%>" style='height: 40px; width: 75px; object-fit: cover;' alt="No Image">
                           <% } %>
                           <% if(data.data[match].team_b_short) {%>
                           <%= data.data[match].team_b_short %>
                           <% } %>
                        </div>
                        <div class="row" style="margin-top: 5px;">
                           <div class="col-6">
                               <% for (let j = 0; j < data.data[match].team_a_scores_over.length; j++) { %>
                                   <% let score_a = data.data[match].team_a_scores_over[j]; %>
                                   <div class="card-title" style="font-size: 15px;">
                                       <span><%= score_a.score %></span><span style="margin-left: 8px;"><%= score_a.over %></span>
                                   </div>
                               <% } %>
                           </div>
                           <div class="col-6" style="text-align: end;">
                               <% for (let k = 0; k < data.data[match].team_b_scores_over.length; k++) { %>
                                   <% let score_b = data.data[match].team_b_scores_over[k]; %>
                                   <div class="card-title" style="font-size: 15px;">
                                       <span><%= score_b.score %></span><span style="margin-left: 8px;"><%= score_b.over %></span>
                                   </div>
                               <% } %>
                           </div>
                       </div>
                        <div class="col-md-4-4 text-center">
                           <% if(data.data[match].match_date) { %>
                           <% if(data.data[match].match_time) { %>
                           <button type="button" style="background-color: #C500E1; border: #C500E1 solid 1px; color: white;" >
                           <%= data.data[match].match_date%> &nbsp;&nbsp;<%= data.data[match].match_time%>
                           </button>
                           <% } %>
                           <% } %>
                        </div>
                     </div>
                  </div>
               </div>
            </div>
            <% } %>  
         </div>
      </div>
      <!-- News CARD VIEW -->
      <span class="fw-bold" style="color: #C500E1; margin: 50px; font-size: 30px;">NEWS FOR YOU</span>
      <div class="container py-5">
         <div class="row row-cols-1 row-cols-md-3 g-4">
            <% if (Array.isArray(news.data) && news.data.length > 0) { %>
            <% news.data.forEach(newsItem => { %>
            <div class="col">
               <div class="card-news news"  style="border:#C500E1 2px solid;" onclick="redirectToNews('<%= newsItem.news_id %><%= newsItem.title %>')">
                  <% if (newsItem.image) { %> 
                  <img src="<%= newsItem.image %>" class="card-img-top" style="height: 250px; " alt="News Image">
                  <% } %>
                  <div class="card-body">
                     <% if (newsItem.pub_date) { %>
                     <button class="rounded text-white text-center" style="background-color: #3B0344; margin-bottom: 8px;"><%= newsItem.pub_date %></button>
                     <% } %>
                     <% if (newsItem.title) { %>
                     <h5 class="card-title" style="color: #3B0344; font-size: 20px;"><%= newsItem.title %></h5>
                     <% } %>
                     <% if (newsItem.description) { %>
                     <h5 class="card-title" style="font-size: 15px;"><%= newsItem.description %></h5>
                     <% } %>
                  </div>
               </div>
            </div>
            <% }); %>
            <% } else { %>
            <p>No news available</p>
            <% } %>
         </div>
      </div>
      <!-- News CARD VIEW -->  
      <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
         integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" 
         crossorigin="anonymous"></script>
      <script src="/js/upcoming.js"></script>
      <script src="/js/newsDetails.js"></script>
      <script>
         $('#owl-carousel').owlCarousel({
         loop: true,
         margin: 30,
         dots: true,
         // nav: true,
         items: 3,})
      </script>
      <%- include('footer'); -%>
   </body>
</html>
