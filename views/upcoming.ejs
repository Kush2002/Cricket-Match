<!DOCTYPE html>
<html lang="en">
   <head>
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1.0">
      <title><%= title%></title>
      <link rel="shortcut icon" href="/img/favicon.ico"/>
      <link rel="stylesheet" href="/css/style.css">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
      <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.css">
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" 
         integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
   <body>
      <%- include('header'); -%>
      <span class="fw-bold" style="color: #C500E1; margin: 50px; font-size: 30px;"><%= text %></span><br>
      <!-- UPCOMING MATCH CARD VIEW -->
      <% let prevMatchDate  = null; %>
      <div class="row">
      <% for (let i = 0; i < data.data.length; i++) { %>
         <% const currentMatchDate = data.data[i].match_date; %>
         <% if (currentMatchDate !== prevMatchDate) { %>
            <% prevMatchDate = currentMatchDate; %>
            <span class="fw-bold" style="color: #C500E1; margin-top: 30px; margin-left:50px;font-size:30px;"><%= currentMatchDate %></span>
            <% } %>
         <div class="item  col-6 col-md-4" id="item" style="margin-bottom: 10px;">
            <div class="card text-white custom-card" onclick="redirectToMatch('<%= data.data[i].match_id %>')" style="background-color: #3B0344; padding: 10px; border-radius: 5px; border:#C500E1 1px solid;">
               <div>
                  <% if(data.data[i].series) {%>
                  <div class="row">
                     <div class="card-title col-12 fw-bold" style="font-size: 13px;">
                        <%= data.data[i].series%>
                     </div>
                  </div>
                  <% } %>
                  <% if(data.data[i].venue) {%>
                    <div class="row">
                       <div class="card-title col-12" style="font-size: 10px;">
                          Venue: <%= data.data[i].venue%>
                       </div>
                    </div>
                    <% } %>
                  <div class="row">
                     <div class="card-title col-4">
                        <% if(data.data[i].team_a_img) {%>
                            <img src="<%=data.data[i].team_a_img%>" class="img-icon" alt="Team A Image">
                        <% } %>
                        <% if(data.data[i].team_a_short) {%>
                        <%= data.data[i].team_a_short %>
                        <% } %>        
                     </div>
                     <div class="card-title col-4">
                        <img src="/img/VS.png" style='height: 45px; width: 45px;' alt="No Image">
                     </div>
                     <div class="card-title col-4">
                        <% if(data.data[i].team_b_short) {%>
                            <%= data.data[i].team_b_short %>
                            <% } %>
                        <% if(data.data[i].team_b_img) {%>
                        <img src="<%=data.data[i].team_b_img%>" class="img-icon" alt="Team B Image">
                        <% } %>
                     </div>
                     <% if(data.data[i].match_type) {%>
                     <% if(data.data[i].matchs) {%>
                        <div class="row">
                           <div class="card-title col-7" style="font-size: 15px;">
                              <%= data.data[i].match_type%>
                           </div>
                           <div class="card-title col-5" style="font-size: 15px; text-align: end;">
                            <%= data.data[i].matchs %>
                         </div>
                        </div>
                        <% } %>
                        <% } %>
                     <div class="col-md-4-4 text-center">
                        <% if(data.data[i].match_date) { %>
                        <% if(data.data[i].match_time) { %>
                        <button type="button" style="background-color: #C500E1; border: #C500E1 solid 1px; color: white;" >
                        <%= data.data[i].match_date%> &nbsp;&nbsp;<%= data.data[i].match_time%>
                        </button>
                        <% } %>
                        <% } %>
                     </div>
                  </div>
               </div>
            </div>
         </div>
         <% } %>  
      </div>    
      <!-- UPCOMING MATCH CARD VIEW -->
      <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
      <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.js"></script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" 
         integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
         <script src="/js/upcoming.js"></script>
         <%- include('footer'); -%>
   </body>
</html>
